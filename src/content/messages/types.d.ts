export interface LocaleContent {
  Metadata: {
    title: string;
  };
  header: {
    buttons: {
      login: string;
      register: string;
    };
  };
  home: {
    metadata: {
      title: string;
    };
    carrousel: {
      description: {
        viewMore: string;
      };
    };
  };
}
